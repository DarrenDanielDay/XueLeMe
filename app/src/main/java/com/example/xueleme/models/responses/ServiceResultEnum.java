package com.example.xueleme.models.responses;

public enum ServiceResultEnum {
    SUCCESS,
    FAIL,
    EXIST,
    NOT_FOUND,
    INVALID,
    VALID,
    AUTHORIZED,
    UNAUTHORIZED,
    TIMED_OUT,
}
